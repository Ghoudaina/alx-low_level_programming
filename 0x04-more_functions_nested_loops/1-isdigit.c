#include "main.h"

/**
 * -isdigit -  it checks if the character is a digit 
 *  Return : 1 for any charcter that is a digit or 0 for any els 
 */

int _isdigit(int x) 

{
	if ( x >= 48 && x <=57 )
 {
 return (1);
 }
 return (0);
}
